// import types from '../actions'
import initialState from '../constants/initialState'

const control = (state = initialState.control, action) => {
  switch (action.payload) {
    default:
      return state
  }
}

export default control
